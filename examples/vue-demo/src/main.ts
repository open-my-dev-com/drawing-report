/**
 * SlipKit Vue 데모 진입점 (F-22).
 * 바닐라 데모와 같은 기능을 Vue로 붙인 예시다 — 붙이는 방법만 다르다.
 */
import { createApp } from 'vue';
import 'slipkit-demo-shared/demo.css';
import App from './App.vue';

createApp(App).mount('#app');
