> 이 문서를 수정하려면 반드시 사용자의 명시적 승인을 먼저 받아야 합니다.

# SlipKit(drawing-report) 개발 규칙

SlipKit은 UI에서 전표 양식을 만들고 값을 입력해 인쇄하거나 PDF로 출력하는 임베드형 패키지
모음(`@omdc-slipkit/*`)입니다. pnpm 모노레포이며 `packages/core`(순수 TS),
`packages/elements`(Lit), `packages/react`, `packages/vue`, `packages/mcp`로 구성됩니다.

이 파일은 저장소 전체 개발 규칙의 진입점입니다. 상세 규칙은 `.claude/rules/`에 있습니다(ADR-024).
현재 상태와 다음 작업은 다음 로드맵에서 확인합니다.

@docs/ROADMAP.md

## 용어

- 규칙에 붙은 **`ADR-xxx`는 `docs/DECISIONS.md`에 기록된 설계 결정 번호**입니다. 결정 내용과 근거,
  채택하지 않은 대안을 확인해야 할 때는 해당 번호를 `DECISIONS.md`에서 찾습니다.

## 규칙 위치

- **브랜치·커밋·PR·작업 시작 절차**: `.claude/rules/branching.md`에 **PR을 올리기 전 자가점검 표**가
  있습니다. PR을 만들거나 갱신하기 전에 모든 항목을 확인합니다.
- **GitHub 이슈 코멘트**: 코멘트를 읽거나 작성하기 전에 `.claude/rules/issue-comments.md`에서
  전체 코멘트의 번호와 상태를 확인하고, 답변·구현·검토 단계에 맞게 기존 코멘트 상태를 수정합니다.
- **용어 사전**: `.claude/rules/terms.md`를 이름을 정하기 전에 확인합니다. 그리드(요소)와
  격자(캔버스 모눈)처럼 혼동하기 쉬운 용어의 구분과 용어 변경 시 확인할 대상을 정리합니다.
- **경로별 상세 규칙**: `.claude/rules/core.md`는 `packages/core/**`, `ui-packages.md`는
  `packages/elements/**`·`packages/react/**`·`packages/vue/**`, `docs.md`는 `docs/**`에 적용합니다.
  각 파일의 `paths` 머리말이 적용 범위를 정합니다.
- **저장소 전체 주석 규칙**: `.claude/rules/comments.md`에서 공개 API와 내부 선언을 포함한 모든
  TSDoc의 작성 기준을 정합니다(ADR-029/059).

## 문서 규칙

- 새 설계 결정은 `docs/DECISIONS.md`에 ADR로 추가합니다. 기존 결정을 번복할 때는 삭제하지 않고
  후속 ADR에서 변경 내용을 기록합니다. ADR 번호는 안정적인 식별자이므로 재사용하지 않습니다.
  세부 기준과 릴리스 정리 예외는 `.claude/rules/docs.md`를 따릅니다.
- `docs/REQUIREMENTS.md`는 `DECISIONS.md`와 일치하도록 함께 갱신합니다.
- 기존 요구사항과 결정으로 답할 수 있는 쟁점은 PM 역할의 AI가 결론을 내립니다. 공개 API, 파일 형식,
  데이터 보존, 제품 동작을 새로 정해야 하는 쟁점만 `docs/OPEN-QUESTIONS.md`에 다음 Q 번호로 추가하고
  사용자에게 확인합니다. 판단 기준은 `issue-comments.md`의 답변 원칙과 같습니다.
- 작업을 완료할 때는 `docs/ROADMAP.md`에 PR 병합 후의 현재 상태와 다음 작업을 기록합니다.

## 작업 원칙

- 기능이나 정보를 추가하기 전에 사용자가 왜 필요한지 확인합니다. 빈 공간을 채우기 위한 항목,
  다른 위치와 중복되는 조작, 목록에서 이미 알 수 있는 값은 추가하지 않습니다.
- 화면의 각 항목은 그 위치에서 대상을 다루는 데 직접 필요해야 합니다. 다른 대상의 설정이나
  파생 정보는 해당 화면에 두지 않습니다.
- 항목의 추가 여부가 제품 동작을 바꾸고 기존 요구사항만으로 결정할 수 없으면 사용자에게 확인합니다.

## 코드 불변 규칙(경로 무관)

- 수식은 자체 파서만 사용합니다. `eval`과 `new Function`은 사용하지 않습니다(ADR-010).
- 문자열을 조합해 모듈 경로를 만드는 동적 import는 패키지와 경로에 관계없이 사용하지 않습니다.
  번들러가 정적으로 추적할 수 있는 리터럴 동적 import만 사용합니다.
- `@omdc-slipkit/core`는 순수 TypeScript로 유지하며 DOM, 브라우저, 프레임워크 API에 의존하지
  않습니다(ADR-002).
- pdfme 타입과 API를 공개 API에 노출하지 않습니다(ADR-016).
- **용어·문구·설정값의 단일 원천 유지**: 한 곳에서 정의되고 여러 곳에서 쓰이는 것(용어,
  UI 문구, 상수 등)을 바꿀 때는 문서(SPEC·DECISIONS·REQUIREMENTS·ROADMAP)·코드 주석·
  사용자 대면 오류 메시지·UI 문구(`strings.ts`)·테스트 설명을 모두 검색해 함께 변경합니다.
  대상 목록과 확정된 용어는 `.claude/rules/terms.md`에서 확인합니다.

## 검증 게이트

- `pnpm verify`가 통과한 상태에서만 커밋합니다. 검증 단계와 실행 순서는 루트 `package.json`의
  `verify` 스크립트에서 관리합니다. lint 단계는 TSDoc 주석 형식도 검사합니다(ADR-030).
  PreToolUse 훅은 `git commit` 전에 이 검증을 실행하고, 실패하면 커밋을 중단합니다.
- 실패하는 테스트를 건너뛰거나 삭제하거나 기준을 완화해서 통과시키지 않습니다.
- `/verify`로 검증을 직접 실행할 수 있고, `/next-task`로 로드맵의 다음 작업을 규칙에 따라
  시작할 수 있습니다.

## 언어

- 설계 문서와 커밋 메시지는 한국어로, 코드 식별자는 영어로 작성합니다. 사용자 대면 메시지는
  영어·한국어·일본어 사전으로 관리하며 영어를 기본값으로 사용합니다. 소스에 특정 언어의 문구를
  직접 작성하지 않습니다(ADR-060).
- 공개 문서인 저장소 `README`와 `docs/guide/*`는 **영어판**을 기준으로 관리합니다. 접미사가 없는
  `*.md`는 영어, `*.ko.md`는 한국어, `*.ja.md`는 일본어입니다(ADR-066). 자세한 기준은
  `.claude/rules/terms.md`를 따릅니다.
- 한국어 문서와 주석은 자연스러운 합니다체로 작성합니다. 문장 일부만 바꾸지 말고 전체 의미와
  흐름을 확인하며, 불필요한 해명·방어적 표현·작업 경위·중복 설명은 제거합니다.
- 한국 개발자가 일반적으로 쓰는 쉽고 분명한 표현을 사용합니다. 어색한 한자어와 번역투는 피하되
  `이미지`, `파일`, `IndexedDB`, `해시`처럼 널리 쓰이는 기술 용어를 억지로 바꾸지 않습니다.
- 같은 대상에는 `.claude/rules/terms.md`에서 정한 이름을 일관되게 사용합니다. 사용자에게 필요하지
  않은 구현 세부사항은 사용자 대면 문구에 넣지 않습니다.
- PR 본문·커밋 메시지에서 기능을 설명할 때 구현 기법이 아니라 사용자 관점의 동작을
  먼저 설명합니다. 예를 들어 "응답 순서 번호로 경쟁 조건을 방지합니다"보다 "전표를 빠르게
  전환해도 마지막으로 선택한 전표의 PDF만 표시합니다"라고 씁니다. 내부 구현은 필요한 경우에만
  덧붙입니다.
