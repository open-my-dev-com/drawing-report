/**
 * 현재 .slip 스키마 버전.
 *
 * @remarks
 * 공개 전 개발 단계라 이 버전이 첫 버전(기준선)이다 — 개발 중 거쳐온 중간 버전은
 * 공개된 적이 없어 하나로 합쳐 0.1.0으로 둔다. 스키마가 바뀔 때마다 올리고, 이전 버전으로부터의
 * 단계를 migrate.ts에 추가한다. 제품 안정 릴리스(공개) 시점에 1.0.0으로 확정한다 (docs/SPEC.md §2).
 */
export const CURRENT_SCHEMA_VERSION = '0.1.0';
